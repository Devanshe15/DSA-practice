def printFibb(n):
        if n ==1: 
            return [1]
        elif n == 2 : 
            return [1,1]
        elif n>2:
            res = [1,1] 
            for i in range(2,n):
                res.append(res[-2]+res[-1])
print(res)           
n= int(input('enter the no'))
print(res)